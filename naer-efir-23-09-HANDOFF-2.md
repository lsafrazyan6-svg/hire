# Передача сессии №2: библиотека референсов «как в эфире»

## Кто и как
Владелица — продуктовый дизайнер, пишет по-русски. Воркспейс: `/Users/lidiasafrazyan/Desktop/воркспейс` (не git). Прочитай сначала `CLAUDE.md` и `SKILLS_MAP.md`, если они там есть. Правила, которые она сама вводила:
- перед любым вопросом дай 2–4 строки сводки контекста, затем один вопрос с рекомендацией; если ответ не меняет решения, не спрашивай;
- не назначай «канон» между сторонними наборами заранее: сравнивай по существу;
- отвечай кратко; не выдумывай значения («нет в источнике»); проверяй по содержимому файлов;
- не копируй длинные куски чужого текста, цитаты до 15 слов;
- **это чужой эфир (Pixel Perfect / Даниил Шишко)** — владелица явно попросила не скачивать видео массово (даже через yt-dlp «для разбора кадров»), просто уважай это и не поднимай тему снова.

## Предыдущая сессия была облачной (claude.ai/code)
Она работала не в воркспейсе на Маке, а в изолированном облачном контейнере, привязанном к чужому по теме GitHub-репозиторию `lsafrazyan6-svg/hire` (сервис поиска работы в IT — никак не связан с этой задачей, использован только как канал передачи файлов, потому что он был подключён к сессии). Видео не смогла скачать: сеть облачного контейнера блокирует youtube.com/googlevideo.com, а сам файл слишком большой для загрузки в чат или в GitHub. Если ты работаешь локально на Маке — у тебя, скорее всего, есть прямой доступ к материалам ниже, это не будет проблемой.

## Материалы
**На Маке (оригинал, должен быть на месте):** `~/.claude/reserve/naer-efir-23-09/` — `stream.mp4` (1080p, 2:01:08), `transcript.txt`/`transcript.vtt` (полная расшифровка, 15 739 слов, whisper large-v3-turbo), `frames/` (кадр n = (n−1)×4 мин), `sheets/` (листы по 6 кадров), `refs-platform-v0.html`.

**Продублировано и разобрано в облачной сессии**, лежит в PR https://github.com/lsafrazyan6-svg/hire/pull/1 (ветка `claude/magical-fermat-574zkl`): тот же `transcript.txt` целиком, `sheet_0–3.png`, `sample_0001–0030.png`, и три готовых документа с результатами разбора (см. ниже). Если решишь продолжать в облаке, а не на Маке — это готовая копия исходников.

**Скиллы с этого же эфира** (бонусный архив, который раздавали в конце стрима) — уже должны быть в `.claude/skills/` воркспейса: `misha-naer-skills`, `wireframe-board`, `jev-fit-test`.

## Главная цель (не изменилась с прошлой передачи)
1. Собрать **идентичную** копию приложения «Reference Library» Миши Наера из эфира 23.09.2026. Идентично по функциям, раскладке, размерам, цветам, текстам интерфейса. Не переносить логотипы, фирменные шрифты и чужое содержимое референсов — нейтральные заглушки.
2. Доска вайрфреймов платформы с отметками ✔/✘/? и правками владелицы.
3. Разобрать, что говорят агенту и как его учат анализировать референсы, записать метод в скиллы (`ds-screen-assembly/references/visual-decomposition-checklist.md`, не плодить дубли).

## Что сделано в прошлой (облачной) сессии

Все три файла ниже — в PR, целиком прочитаны и разобраны по кадрам/транскрипту, не выдуманы:

- **`naer-efir-23-09-visual-spec-draft.md`** — анатомия экрана «Библиотека» (сайдбар, хедер, фильтры, карточка), вьюер «Путь продукта» с таблицей точных параметров (px, тайминги) из документа «Поведение панелей», который был виден на кадре крупным планом.
- **`naer-efir-23-09-speech-analysis.md`** — разбор речи: главный принцип (агент не оценивает «красиво» сам, только раскладывает по полочкам объяснение владельца), пайплайн разбор→сборка, приём поиска референсов через Grok/X, ограничения агента.
- **`naer-efir-23-09-core-logic.md`** — самый свежий и главный из трёх: сведена вся логика разбора референса в одном месте, с таблицей «что уже реализовано / что нет».

**Ключевая находка**, которая закрыла два открытых вопроса: три точки-индикатора под карточкой — это прогресс по стадиям (исходник → разобран → собран), не декор; а вьюер «Путь продукта» (127.0.0.1:4380) — это НЕ экран самого продукта «Библиотека референсов», а отдельный мета-скилл Дани «Canvas» для журналирования любого проекта. Для идентичного клона его, скорее всего, копировать не нужно.

**Рабочий прототип-артефакт (Artifact, не файл в репозитории):**
https://claude.ai/artifact/EkNKuEcizYiMY26NWKeZsX — «Reference Library». Обновлён в этой же сессии (не с нуля — переписана вёрстка поверх существующей базы данных, старые данные сохранены). Реализовано:
- сайдбар: Reference Library → Библиотека/К сборке/К просмотру → Подборки (пользовательские папки) → Корзина/Настройки — по образцу реальных кадров;
- карточка: бейдж типа (Изображение/Видео), подпись-имя файла, индикатор стадии;
- разбор референса **реально вызывает Claude** (через `sample` capability): требует сначала текстовое «что нравится и почему» (без этого не пускает — прямое требование из транскрипта), возвращает теги, подборку (только из уже существующих, не выдумывает новую), механику, и приёмы в форме «работает без картинки» — с кнопкой «Скопировать как Markdown» для твоего файла принципов;
- «Собрано» (стадия 3) — честно ручная отметка: реальная сборка компонента идёт через MCP в кодовом агенте, веб-страница Artifact физически не может это выполнить.

Технически: Artifact contract 0.2.54, capabilities `db`+`assets`+`sample`+`user`. Обновлять этот же артефакт — публиковать с `url: "https://claude.ai/artifact/EkNKuEcizYiMY26NWKeZsX"`, не создавать новый.

**Доска вайрфреймов** (отдельный артефакт, сделана раньше остального — до того, как пришли реальные кадры, по одному текстовому описанию из первой версии HANDOFF): https://claude.ai/artifact/3qMX6DZiAcapv2Zye6fkYJ. Отметки ✔/✘/? хранятся в `db`, читаются через `ArtifactData`. **Она устарела** относительно реальных кадров — стоит сверить и переделать по `naer-efir-23-09-visual-spec-draft.md`, а не оставлять как есть.

## Статус
- Готово: расшифровка разобрана целиком, кадры разобраны, рабочий прототип с реальной логикой разбора референса, три документа-разбора.
- **Не сделано:**
  - точные HEX-цвета и px-сетка — сейчас оценка на глаз по кадрам, не по пикселю (для точности нужен сам `stream.mp4` или кадры покрупнее — которых физически не было в облачной сессии);
  - остальные узлы карты «Путь продукта» (кроме «Поведение панелей») — нечитаемы в имеющемся разрешении кадров;
  - доска вайрфреймов не пересобрана под реальные кадры (см. выше);
  - «сайтик» Шишко (просмотрщик пути), обещан ранее, не начат;
  - метод разбора речи ещё не перенесён в `ds-screen-assembly/references/visual-decomposition-checklist.md` — материал для этого есть в `naer-efir-23-09-speech-analysis.md`, осталось дописать в существующий файл.
- **Открытый вопрос владелице** (не подтверждено кадром/речью, моя интерпретация): что именно делают пункты «К сборке» и «К просмотру» в сайдбаре. Сейчас в артефакте это фильтр по стадии референса (К просмотру = только добавленные, К сборке = разобранные). Если знаешь точно — поправь.
- Ждут решений владелицы, унаследовано из прошлой передачи (не проверялось в этой сессии): layout-предложения в `references/интерфейсы/layout-сравнение.md`; восемь выборов по анимации в `references/моушн/анимация-по-элементам.md`; `better-colors`; `ponytail-review/audit`.

## Технические заметки
- Agentные лимиты сессии сбрасываются в 18:30 по Москве (унаследовано из прошлой передачи, не проверялось).
- Скиллы и материалы без лицензии — вне воркспейса (`~/.claude/skills`, `~/.claude/reserve`).
- Перед записью в Artifact — загрузи скиллы `artifact-design` и `artifact-capabilities`; базу читает и пишет `ArtifactData`.
