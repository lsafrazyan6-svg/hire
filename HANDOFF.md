# Передача сессии: библиотека референсов «как в эфире»

## Кто и как
Владелица — продуктовый дизайнер, пишет по-русски. Воркспейс: `/Users/lidiasafrazyan/Desktop/воркспейс` (не git). Прочитай сначала `CLAUDE.md` и `SKILLS_MAP.md`. Правила, которые она сама вводила:
- перед любым вопросом дай 2–4 строки сводки контекста (что просила, что сделано, что предполагаешь), затем один вопрос с рекомендацией; если ответ не меняет решения, не спрашивай;
- не назначай «канон» между сторонними наборами заранее: сравнивай по существу, бери лучшее, свои правила тоже сравнивай, конфликты показывай таблицей;
- отвечай кратко; не выдумывай значения («нет в источнике»); проверяй по содержимому файлов, а не по размеру или названию;
- не копируй длинные куски чужого текста, цитаты до 15 слов.

## Главная цель
1. Собрать **идентичную** копию приложения «Reference Library» Миши Наера (Сбер) из эфира 23.09.2026 «GPT-6 Astra + Миша Наер из Сбера: тест в реальной задаче дизайнера». Идентично по функциям, раскладке, размерам, цветам, текстам интерфейса. Не переносить логотипы, фирменные шрифты (SB Sans) и чужое содержимое референсов, вместо них нейтральные заглушки.
2. Отдельной страницей: доска вайрфреймов этой платформы с отметками ✔/✘/? и правками владелицы (по скиллу `wireframe-board`, отметки хранить в `db` Artifact, чтобы их можно было прочитать через ArtifactData).
3. Разобрать, **что говорят агенту и как его учат анализировать референсы** (промпты, схема разбора, правила), сделать выводы из эфира и записать метод в скиллы так же, как ранее механику градиента (дополнять `ds-screen-assembly/references/visual-decomposition-checklist.md`, не плодить дубли).

## Материалы (постоянная копия)
`~/.claude/reserve/naer-efir-23-09/`: `stream.mp4` (1080p, 2:01:08), `transcript.txt` и `transcript.vtt` (15 739 слов, whisper large-v3-turbo), `frames/` (кадр n = (n−1)×4 мин), `sheets/` (листы по 6 кадров), `refs-platform-v0.html` (исходник первой версии).

## Что видно на кадрах (проверено по листам, речь пока не разобрана)
До ~16 мин слайды и разговор. Приложение показано примерно на 16–58 мин и 72–82 мин: тёмный интерфейс, боковая панель («Reference Library», «Библиотека», «К сборке», «К просмотру», «Подборки» со списком тем, «Корзина», «Настройки»), заголовок «Библиотека» с кнопкой «+ Добавить», поиск «Найти референс», фильтры «Стадия», «Фильтры», «Вид и порядок», сетка карточек с плашками «Изображение»/«Видео». Карточка референса открывается окном с вкладками, есть экран «Разбираю референс» (ИИ-разбор) и сборка React-компонента из видео («v1 · React web · выбрана», слайд «От видео к компоненту» ~24 мин). Есть страница «Путь продукта» с вкладкой «Артефакты» и документом «Поведение панелей» (~72 мин). Слайд ~80 мин: «Какой контекст нужен агенту, чтобы повторить эффект без угадывания?».

## Статус
- Готово: скачивание, расшифровка, кадры. Первая версия библиотеки опубликована как Artifact: https://claude.ai/artifact/EkNKuEcizYiMY26NWKeZsX (db, assets, sample, user; три «примера» в базе). Это **не** копия эфира, а общий прототип; переписать по спецификации.
- **Не сделано:** (а) разбор речи про указания агенту и логику приложения; (б) точная визуальная спецификация (агент упал по лимиту, результата нет: раскладка в px, цвета с пикселей, все тексты, поля данных, состояния, сценарии, ключевые кадры в `…/ui/`); (в) страница вайрфреймов с правками; (г) метод анализа референсов в скиллы; (д) «сайтик» Шишко (просмотрщик пути), обещан раньше.
- Ждут решений владелицы: layout-предложения (пять спорных пунктов) в `references/интерфейсы/layout-сравнение.md`; восемь выборов по анимации в `references/моушн/анимация-по-элементам.md` и создание скилла `motion-reference`; `better-colors`; `ponytail-review/audit`.

## Технические заметки
- Скачивание: `yt-dlp --cookies-from-browser chrome`; при ошибке формата брать `bv*[height<=1080]+ba/b`.
- Artifact: перед написанием загрузи скиллы `artifact-design` и `artifact-capabilities`; базу читает и пишет инструмент ArtifactData (загрузить через ToolSearch). Локальную проверку страницы делал временной конфигурацией в `.claude/launch.json` (потом убрать).
- Агентные лимиты сессии сбрасываются в 18:30 по Москве.
- Скиллы и материалы без лицензии лежат вне воркспейса (`~/.claude/skills`, `~/.claude/reserve`), потому что `workspace-template` копирует `.claude/skills`, `references/`, `library/`.
- Сравнения по анимации, layout и остатку Krehel уже в `references/`; в них есть предложения, которые не применены.
